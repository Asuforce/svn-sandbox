# svn-sandbox

## Usage

```sh
# Checkout
$ svn co https://github.com/Asuforce/svn-sandbox

# Create branch
$ svn copy trunk branches/hoge

# Add new file
$ svn add filename

# Show status
$ svn status

# Commit
$ svn commit -m "Commit message"
```

